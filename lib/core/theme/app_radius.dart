abstract final class AppRadius {
  static const double badge       = 8;    // chips, tags pequenas
  static const double input       = 999;  // padrão único: campos e botões de formulário
  static const double textarea    = 16;   // campos multilinha (textarea), evita o efeito "pílula"
  static const double card        = 16;   // cards padrão
  static const double thumbnail   = 14;   // miniaturas/ícones de item (thumbnail de música, medley)
  static const double cardHero    = 18;   // cards de item em destaque (música, medley, evento)
  static const double cardLarge   = 20;   // cards grandes
  static const double bottomSheet = 24;   // bottom sheets deslizantes
  static const double sheet       = 28;   // containers de sheet e ícones
  static const double pill        = 999;  // reservado para botões cápsula especiais
}
